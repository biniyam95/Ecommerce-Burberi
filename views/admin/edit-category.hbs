
<h4 class="fw-bold py-2 mb-4 px-4"><span class="text-muted fw-bold">/ CATEGORY LIST </span> </h4>


              {{!-- ============================================== modified===================================================== --}}
               <!-- Basic Layout -->
              <div class="row">
                <div class="col-xl">
                  <div class="card mb-4">
                    <div class="card-header d-flex justify-content-between align-items-center">
                      
                    </div>
                    <div class="card-body">
                     
                      {{!-- form --}}
                      <form action="/admin/edit-category/{{fetchedOneCategoryf._id}}" method="post">
 

                         {{!-- category --}}
                        <div class="mb-3">
                          <label class="form-label text-info" for="">Category Name</label>
                           <input type="text" class="form-control" name="categoryName" id="categoryName"  value="{{fetchedOneCategoryf.categoryName}}" >
                        </div>

                        
                        
                        {{!-- submit --}}
                        
                        <button type="submit" class="btn btn-warning text-dark">Save</button>
                         <button  class="btn btn-secondary" ><a href="/admin/category-List" class="text-white" >Cancel</a></button>

                      </form>


                    </div>
                  </div>
                </div>
                 
                <div class="col-xl">
                  {{!-- adjacent column in the same row as above form--}}
                </div>
              </div>

